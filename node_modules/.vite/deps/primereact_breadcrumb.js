import {
  ChevronRightIcon
} from "./chunk-5TEMSYAQ.js";
import "./chunk-2A7SLJML.js";
import {
  ComponentBase
} from "./chunk-5T6K3NY7.js";
import {
  IconUtils,
  ObjectUtils,
  PrimeReactContext,
  classNames,
  mergeProps
} from "./chunk-KUBAI3MN.js";
import {
  require_react
} from "./chunk-CANBAPAS.js";
import {
  __toESM
} from "./chunk-5WRI5ZAA.js";

// node_modules/primereact/breadcrumb/breadcrumb.esm.js
var React = __toESM(require_react());
function _typeof(obj) {
  "@babel/helpers - typeof";
  return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(obj2) {
    return typeof obj2;
  } : function(obj2) {
    return obj2 && "function" == typeof Symbol && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
  }, _typeof(obj);
}
function _toPrimitive(input, hint) {
  if (_typeof(input) !== "object" || input === null) return input;
  var prim = input[Symbol.toPrimitive];
  if (prim !== void 0) {
    var res = prim.call(input, hint || "default");
    if (_typeof(res) !== "object") return res;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (hint === "string" ? String : Number)(input);
}
function _toPropertyKey(arg) {
  var key = _toPrimitive(arg, "string");
  return _typeof(key) === "symbol" ? key : String(key);
}
function _defineProperty(obj, key, value) {
  key = _toPropertyKey(key);
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }
  return obj;
}
var BreadCrumbBase = ComponentBase.extend({
  defaultProps: {
    __TYPE: "BreadCrumb",
    id: null,
    model: null,
    home: null,
    separatorIcon: null,
    style: null,
    className: null,
    children: void 0
  }
});
function ownKeys(object, enumerableOnly) {
  var keys = Object.keys(object);
  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    enumerableOnly && (symbols = symbols.filter(function(sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    })), keys.push.apply(keys, symbols);
  }
  return keys;
}
function _objectSpread(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = null != arguments[i] ? arguments[i] : {};
    i % 2 ? ownKeys(Object(source), true).forEach(function(key) {
      _defineProperty(target, key, source[key]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function(key) {
      Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
    });
  }
  return target;
}
var BreadCrumb = React.memo(React.forwardRef(function(inProps, ref) {
  var context = React.useContext(PrimeReactContext);
  var props = BreadCrumbBase.getProps(inProps, context);
  var _BreadCrumbBase$setMe = BreadCrumbBase.setMetaData({
    props
  }), ptm = _BreadCrumbBase$setMe.ptm;
  var elementRef = React.useRef(null);
  var itemClick = function itemClick2(event, item) {
    if (item.disabled) {
      event.preventDefault();
      return;
    }
    if (!item.url) {
      event.preventDefault();
    }
    if (item.command) {
      item.command({
        originalEvent: event,
        item
      });
    }
  };
  var createHome = function createHome2() {
    var home2 = props.home;
    if (home2) {
      if (home2.visible === false) {
        return null;
      }
      var _icon = home2.icon, target = home2.target, url = home2.url, disabled = home2.disabled, style = home2.style, _className = home2.className, template = home2.template, _label = home2.label;
      var _className2 = classNames("p-breadcrumb-home", {
        "p-disabled": disabled
      }, _className);
      var iconProps = mergeProps({
        className: "p-menuitem-icon"
      }, ptm("icon"));
      var icon = IconUtils.getJSXIcon(_icon, _objectSpread({}, iconProps), {
        props
      });
      var actionProps = mergeProps({
        href: url || "#",
        className: "p-menuitem-link",
        "aria-disabled": disabled,
        target,
        onClick: function onClick(event) {
          return itemClick(event, home2);
        }
      }, ptm("action"));
      var labelProps = mergeProps({
        className: "p-menuitem-text"
      }, ptm("label"));
      var label = _label && React.createElement("span", labelProps, _label);
      var content = React.createElement("a", actionProps, icon, label);
      if (template) {
        var defaultContentOptions = {
          onClick: function onClick(event) {
            return itemClick(event, home2);
          },
          className: "p-menuitem-link",
          labelClassName: "p-menuitem-text",
          element: content,
          props
        };
        content = ObjectUtils.getJSXElement(template, home2, defaultContentOptions);
      }
      var menuitemProps = mergeProps({
        className: _className2,
        style
      }, ptm("menuitem"));
      return React.createElement("li", menuitemProps, content);
    }
    return null;
  };
  var createSeparator = function createSeparator2() {
    var iconClassName = "p-breadcrumb-chevron";
    var separatorIconProps = mergeProps({
      className: iconClassName
    }, ptm("separatorIcon"));
    var icon = props.separatorIcon || React.createElement(ChevronRightIcon, separatorIconProps);
    var separatorIcon = IconUtils.getJSXIcon(icon, _objectSpread({}, separatorIconProps), {
      props
    });
    var separatorProps = mergeProps({
      className: "p-menuitem-separator"
    }, ptm("separator"));
    return React.createElement("li", separatorProps, separatorIcon);
  };
  var createMenuitem = function createMenuitem2(item) {
    if (item.visible === false) {
      return null;
    }
    var className2 = classNames("p-menuitem", item.className, {
      "p-disabled": item.disabled
    });
    var labelProps = mergeProps({
      className: "p-menuitem-text"
    }, ptm("label"));
    var label = item.label && React.createElement("span", labelProps, item.label);
    var actionProps = mergeProps({
      href: item.url || "#",
      className: "p-menuitem-link",
      target: item.target,
      onClick: function onClick(event) {
        return itemClick(event, item);
      },
      "aria-disabled": item.disabled
    }, ptm("action"));
    var content = React.createElement("a", actionProps, label);
    if (item.template) {
      var defaultContentOptions = {
        onClick: function onClick(event) {
          return itemClick(event, item);
        },
        className: "p-menuitem-link",
        labelClassName: "p-menuitem-text",
        element: content,
        props
      };
      content = ObjectUtils.getJSXElement(item.template, item, defaultContentOptions);
    }
    var menuitemProps = mergeProps({
      className: className2,
      style: item.style
    }, ptm("menuitem"));
    return React.createElement("li", menuitemProps, content);
  };
  var createMenuitems = function createMenuitems2() {
    if (props.model) {
      var _items = props.model.map(function(item, index) {
        if (item.visible === false) {
          return null;
        }
        var menuitem = createMenuitem(item);
        var separator2 = index === props.model.length - 1 ? null : createSeparator();
        var key = item.label + "_" + index;
        return React.createElement(React.Fragment, {
          key
        }, menuitem, separator2);
      });
      return _items;
    }
    return null;
  };
  React.useImperativeHandle(ref, function() {
    return {
      props,
      getElement: function getElement() {
        return elementRef.current;
      }
    };
  });
  var className = classNames("p-breadcrumb p-component", props.className);
  var home = createHome();
  var items = createMenuitems();
  var separator = createSeparator();
  var menuProps = mergeProps({
    className: "p-breadcrumb-list"
  }, ptm("menu"));
  var rootProps = mergeProps({
    id: props.id,
    ref: elementRef,
    className,
    style: props.style,
    "aria-label": "Breadcrumb"
  }, BreadCrumbBase.getOtherProps(props), ptm("root"));
  return React.createElement("nav", rootProps, React.createElement("ul", menuProps, home, separator, items));
}));
BreadCrumb.displayName = "BreadCrumb";
export {
  BreadCrumb
};
//# sourceMappingURL=primereact_breadcrumb.js.map
